---
title: T4 텍스트 템플릿 디버그 | Microsoft Docs
ms.custom: ''
ms.date: 11/15/2016
ms.prod: visual-studio-tfs-dev14
ms.reviewer: ''
ms.suite: ''
ms.tgt_pltfrm: ''
ms.topic: article
helpviewer_keywords:
- text templates, troubleshooting
- text templates, debugging
ms.assetid: 0877fdf2-20bf-42da-b3cc-4c5856b80821
caps.latest.revision: 30
author: gewarren
ms.author: gewarren
manager: douge
ms.openlocfilehash: f299b89f7f59cbfc043bb77e6e56c3e5fac22d16
ms.sourcegitcommit: 9ceaf69568d61023868ced59108ae4dd46f720ab
ms.translationtype: MT
ms.contentlocale: ko-KR
ms.lasthandoff: 10/12/2018
ms.locfileid: "49298911"
---
# <a name="debugging-a-t4-text-template"></a>T4 텍스트 템플릿 디버깅
[!INCLUDE[vs2017banner](../includes/vs2017banner.md)]

텍스트 템플릿에서 중단점을 설정할 수 있습니다. 디자인 타임 텍스트 템플릿 디버그 텍스트 템플릿 파일을 저장 하 고 선택한 **T4 템플릿 디버그** 솔루션 탐색기에서 파일의 바로 가기 메뉴. 런타임 텍스트 템플릿 디버깅을 단순히 속해 있는 응용 프로그램을 디버깅 합니다.  
  
 텍스트 템플릿을 디버그 하려면 템플릿 변형 프로세스의 단계를 이해 해야 합니다. 다른 종류의 오류는 각 단계 내에서 발생할 수 있습니다. 단계는 다음과 같습니다.  
  
|단계|디자인 타임 템플릿을: 발생 하면|런타임 템플릿을: 발생 하면|  
|----------|--------------------------------------------|-----------------------------------------|  
|코드가는 텍스트 템플릿에서 생성 됩니다.<br /><br /> 지시문에서 오류 또는 일치 하지 않거나 disordered `<#…#>` 태그입니다.|서식 파일을 저장 하거나 텍스트 변환 호출 합니다.|서식 파일을 저장 하거나 텍스트 변환 호출 합니다.|  
|생성 된 코드는 컴파일됩니다.<br /><br /> 템플릿 코드에서 컴파일 오류입니다.|이전 단계 직후.|응용 프로그램 코드와 함께|  
|코드 실행합니다.<br /><br /> 템플릿 코드에 런타임 오류입니다.|이전 단계 직후.|때 응용 프로그램을 실행 하 고 템플릿 코드를 호출 합니다.|  
  
 대부분의 경우에서 템플릿 코드에 줄 번호는 오류 보고서에 제공 됩니다. 오류 보고서를 임시 파일 이름으로 참조 하는 경우 일반적인 원인은 텍스트 템플릿의 코드에서 짝이 맞지 않는 괄호입니다.  
  
 텍스트 템플릿에 중단점을 설정 하 고 일반적인 방법으로 디버그할 수 있습니다.  
  
## <a name="common-errors-and-fixes"></a>일반적인 오류 및 수정  
 다음 표에서 가장 일반적인 오류 및 해당 수정 합니다.  
  
|오류 메시지|설명|솔루션|  
|-------------------|-----------------|--------------|  
|기본 클래스를 로드 하지 못했습니다{0}' 변환을 클래스 상속 합니다.|에 지정 된 기본 클래스를 찾을 수 없는 경우 발생 합니다 `inherits` 템플릿 지시문에 대 한 매개 변수입니다. 메시지는 템플릿 지시문의 줄 번호를 제공합니다.|지정 된 클래스가 있고에 있는 어셈블리는 어셈블리 지시문에 지정 되어 있는지 확인 해야 합니다.|  
|확인 하지 못했습니다 파일에 텍스트를 포함 합니다.{0}|포함된 된 템플릿을 찾을 수 없는 경우 발생 합니다. 메시지 요청 된 포함 파일의 이름을 제공합니다.|원래 템플릿 경로 상대적인 파일 경로 호스트에 등록 되어 있는 위치에 파일 인지 또는 파일에 전체 경로 확인 해야 합니다.|  
|변환 개체를 초기화 하는 동안 오류가 발생 했습니다. 변환 실행 되지 않습니다.|에 있는 initialize (')' 변환 클래스의 실패 또는 false를 반환 하는 경우 발생 합니다.|에 지정 된 기본 변환 클래스에서 initialize () 함수에서 코드를 제공 합니다 \<#@template#> 지시문 또는 지시문 프로세서. 초기화 실패를 발생 시킨 오류는 오류 목록입니다. 실패 한 이유를 조사 합니다. 서식 파일을 디버그 하는 절차를 수행 하 여 initialize ()에 대 한 실제 생성 된 코드를 살펴볼 수 있습니다.|  
|어셈블리 '{0}'에 지시문 프로세서'{1}' FullTrust 권한 집합이 부여 되지 않은 합니다. 신뢰할 수 있는 어셈블리만 지시문 프로세서를 제공할 수 있습니다. 이 지시문 프로세서를 로드 하지 않습니다.|시스템 지시문 프로세서를 포함 하는 어셈블리에 FullTrust 권한을 부여 하지 않을 때 발생 합니다. 메시지에 지시문 프로세서의 이름과 어셈블리의 이름을 제공합니다.|로컬 컴퓨터의 신뢰할 수 있는 어셈블리 에서만 사용 해야 합니다.|  
|경로 '{0}'이 컴퓨터에 로컬 이거나 신뢰할 수 있는 영역의 일부 여야 합니다.|지시문 또는 어셈블리 지시문을 파일에 로컬 컴퓨터에 없는 컴퓨터나 네트워크의 신뢰할 수 있는 영역을 참조 하는 경우 발생 합니다.|신뢰할 수 있는 영역에서 지시문 또는 어셈블리 지시문 있는 디렉터리를 표시 해야 합니다. Internet Explorer를 통해 신뢰할 수 있는 영역에 네트워크 디렉터리를 추가할 수 있습니다.|  
|"잘못 된 토큰 ' catch'" 또는 "네임 스페이스 멤버를 포함할 직접 수 없습니다"와 같은 여러 구문 오류|템플릿 코드에 닫는 중괄호가 너무 많습니다. 컴파일러를 표준 생성 코드와 혼동 됩니다.|구분 기호 코드 안의 대괄호와 중괄호를 닫기 횟수를 확인 합니다.|  
|루프 또는 조건부 컴파일 또는 올바르게 실행 되지 않습니다. 예: `<#if (i>10)#> Number is: <#= i #>`<br /><br /> 이 코드는 항상 값을 출력 i. 만 "수:"는 조건부입니다.|C#에서는 항상를 제어문에 포함 된 텍스트 블록을 묶는 중괄호를 사용 합니다.|중괄호 추가: `<#if (i>10) { #>    Number is: <#= i #><# } #>`합니다.|  
|"너무 복잡 한 식" 경우 전처리 된 런타임 템플릿으로 컴파일 또는 디자인 타임 템플릿을 처리 합니다.<br /><br /> [!INCLUDE[vsprvs](../includes/vsprvs-md.md)] 런타임 템플릿에서 생성 된 코드를 검사 하려고 할 때 작동을 멈춥니다.|텍스트 블록 너무 깁니다. T4는 템플릿 줄에 하나의 문자열 리터럴을 포함 하는 문자열 연결 식을 텍스트 블록으로 변환합니다. 매우 긴 텍스트 블록에는 컴파일러의 크기 제한 한도가 있습니다.|같은 식 블록을 사용 하 여 긴 텍스트 블록을 중단 합니다.<br /><br /> `<#= "" #>`|  
  
## <a name="warning-descriptions-and-fixes"></a>경고 설명 및 수정  
 다음 표에서 사용 가능한 경우 수정 함께 가장 일반적인 경고를 나열 합니다.  
  
|경고 메시지|설명|솔루션|  
|---------------------|-----------------|--------------|  
|포함 파일을 로드 '{0}'는 null 또는 빈 문자열을 반환 합니다.|포함 된 텍스트 템플릿 파일을이 비어 있으면 발생 합니다. 메시지에 포함된 된 파일의 파일 이름을 제공합니다.|Include 지시문을 제거 하거나 파일에 일부 콘텐츠가 있는지 확인 하세요.|  
|변환을 컴파일하는 중:|모든 오류 또는 경고 변환 컴파일할 때 컴파일러에서 생성 된이 문자열 앞에 추가 합니다. 이 문자열은 컴파일러에서 오류 또는 경고를 의미 합니다.|DLL을 찾는 데 문제가 있으면 DLL이 GAC에 있으면 전체 경로 또는 정규화 된 강력한 이름을 제공 해야 합니다.|  
|매개 변수 '{0}' 지시문에 이미 있습니다. 중복 된 매개 변수는 무시 됩니다.|지시문에는 매개 변수를 두 번 이상 지정 하는 경우 발생 합니다. 메시지 매개 변수 이름과 지시문의 줄 번호를 제공합니다.|중복 된 매개 변수 지정을 제거 합니다.|  
|포함 파일을 로드 하는 오류가 발생 했습니다 '{0}'. Include 지시문은 무시 됩니다.|에 지정 된 파일을 찾을 수 없을 때 발생 한 `include` 지시문입니다. 메시지 파일 및 줄 번호 지시문의 이름을 제공합니다.|포함 파일에 등록 된 호스트 포함 디렉터리 중 하나에서 또는 원래 텍스트 템플릿 파일을 동일한 디렉터리에 존재 해야 합니다.|  
|잘못 된 기본 클래스가 Transformation 클래스에 대 한 지정 되었습니다. 기본 클래스는 microsoft.visualstudio.texttemplating.texttransformation과에서 파생 되어야 합니다.|발생 경우 합니다 `inherits` 에서 상속 되지 않는 클래스를 지정 하는 매개 변수 템플릿 지시문에서 `TextTransformation`합니다. 메시지는 템플릿 지시문의 줄 번호를 제공합니다.|파생 된 클래스를 지정 `TextTransformation`합니다.|  
|'Template' 지시문에 잘못 된 문화권을 지정 했습니다. 문화권은 "XX-XX" 형식에서 이어야 합니다. 고정 문화권이 사용 됩니다.|템플릿 지시문에서 culture 매개 변수를 올바르게 지정 하는 경우 발생 합니다. 메시지는 템플릿 지시문의 줄 번호를 제공합니다.|"XX-XX" 형식으로 유효한 문화권으로 culture 매개 변수를 변경 합니다.|  
|잘못 된 디버그 값을 '{0}' template 지시문에 지정 되었습니다. 디버그 값은 "true" 또는 "false" 여야 합니다. 기본값은 "false" 됩니다.|발생 경우를 `debug` 템플릿 지시문에서 매개 변수를 올바르게 지정 합니다. 메시지는 템플릿 지시문의 줄 번호를 제공합니다.|"True" 또는 "false"로 디버그 매개 변수를 설정 합니다.|  
|잘못 된 HostSpecific 값 '{0}' template 지시문에 지정 되었습니다. HostSpecific 값 "true" 또는 "false" 여야 합니다. 기본값은 "false" 됩니다.|발생 경우 호스트 관련 매개 변수는 `template` 지시문 잘못 지정 되었습니다. 메시지는 템플릿 지시문의 줄 번호를 제공합니다.|"True" 또는 "false"로 호스트 관련 매개 변수를 설정 합니다.|  
|잘못 된 언어 '{0}'는 'template' 지시문에 지정 되었습니다. 언어는 "C#" 또는 "VB" 여야 합니다. 기본값은 "C#" 됩니다.|에 지원 되지 않는 언어를 지정 하는 경우 발생 합니다 `template` 지시문입니다. 만 "C#" 또는 "VB" (대/소문자 구분) 허용 됩니다. 메시지는 템플릿 지시문의 줄 번호를 제공합니다.|설정 된 `language` "C#" 또는 "VB" template 지시문에 대 한 매개 변수입니다.|  
|여러 출력 지시문이 템플릿에서 발견 되었습니다. 첫 번째를 제외한 모든 무시 됩니다.|발생 경우 여러 `output` 지시문은 템플릿 파일에 지정 됩니다. 메시지 중복 출력 지시문의 줄 번호를 제공 합니다.|중복 제거 `output` 지시문입니다.|  
|여러 템플릿 지시문 템플릿에서 찾을 수 없습니다. 첫 번째를 제외한 모든 무시 됩니다. Template 지시문에 여러 매개 변수가 하나의 template 지시문 내에서 지정 해야 합니다.|여러 개 지정 하는 경우에 발생 `template` 텍스트 템플릿 파일 (포함된 파일 포함) 내에서 지시문입니다. 메시지는 중복 된 템플릿 지시문의 줄 번호를 제공합니다.|다양 한 집계 `template` 하나로 지시문 `template` 지시문입니다.|  
|지시문에 대 한 지정 된 프로세서가 없습니다 '{0}'. 지시문은 무시 됩니다.|지정 하는 경우 발생을 `custom` 지시문을 제공 하지 않습니다 하지만 `processor` 특성입니다. 메시지 이름 지시문 및 줄 번호를 제공합니다.|제공을 `processor` 특성의 이름으로는 `directive` 지시문에 대 한 프로세서.|  
|프로세서 '{0}'지시문에 대 한 찾을 수 없습니다'{1}'. 지시문은 무시 됩니다.|시스템에서 찾을 수 없을 때 발생 합니다 `directive` 내에서 지정 된 프로세서는 `custom` 지시문입니다. 메시지 지시문 이름, 프로세서 이름과 지시문의 줄 번호를 제공합니다.|설정 된 `processor` 지시문 프로세서의 이름으로 지시문의 특성입니다.|  
|필수 매개 변수 '{0}'for '지시문{1}' 찾을 수 없습니다. 지시문은 무시 됩니다.|시스템 필수 지시문 매개 변수를 제공 하지 않는 경우 발생 합니다. 메시지는 누락 된 매개 변수의 이름, 지시문 이름 및 줄 번호를 제공합니다.|누락 된 매개 변수를 제공 합니다.|  
|프로세서 '{0}'지시문을 지원 하지 않습니다'{1}'. 지시문은 무시 됩니다.|지시문 프로세서 지시문을 지원 하지 않는 경우 발생 합니다. 메시지는 지시문 프로세서의 이름과 함께 잘못 된 지시문의 이름 및 줄 번호를 제공 합니다.|지시문의 이름을 수정 합니다.|  
|파일에 대 한 include 지시문 '{0}' 인해 무한 루프가 발생 합니다.|순환 지시문을 포함 하는 경우에 표시 된 (예를 들어: 파일 A는 파일을 포함 하는 파일 B 포함).|순환 지정 하지 않는 include 지시문입니다.|  
|변환 실행:|모든 오류 또는 경고 변환을 실행 하는 동안 생성 된이 문자열 앞에 추가 합니다.|해당 사항 없음.|  
|블록 내에서 예기치 않은 시작 또는 끝 태그를 찾을 수 있습니다. 시작 또는 끝 태그를 잘못 입력 하지 않은 모든 블록이 중첩 된 템플릿에서 않아도 된다는 있는지 확인 합니다.|예기치 않은 경우 표시 \<# 또는 #>. 즉, 있는 경우는 \<닫혀 있지 다른 열린 태그 뒤에 # 보유 하 신 #> 되기 전에 열기 태그 없음 닫히지 않은 경우. 메시지는 일치 하지 않는 태그의 줄 번호를 제공합니다.|일치 하지 않는 시작 또는 끝 태그를 제거 하거나 이스케이프 문자를 사용 합니다.|  
|지시문의 형식이 잘못 지정 되었습니다. 지시문은 무시 됩니다. 형식에서 지시문을 지정 하십시오 `<#@ name [parametername="parametervalue"]*  #>`|지시문을 올바른 형식으로 지정 하지 않으면 파서에서 표시 합니다. 메시지는 잘못 된 지시문의 줄 번호를 제공합니다.|모든 지시문 형식에서 확인 `<#@ name [parametername="parametervalue"]*  #>`합니다. 자세한 내용은 [T4 텍스트 템플릿 지시문](../modeling/t4-text-template-directives.md)합니다.|  
|어셈블리를 로드 하지 못했습니다 '{0}'에 대해 등록 된 지시문 프로세서'{1}'<br /><br /> {2}|지시문 프로세서를 로드할 수 없는 호스트에서 때 발생 합니다. 지시문 프로세서에 지시문 프로세서의 이름을 제공 하는 어셈블리를 식별 하는 메시지입니다.|지시문 프로세서 올바르게 등록 되 고 어셈블리에 존재 하는지 확인 해야 합니다.|  
|유형을 찾지 못했습니다. '{0}'Assembly' in에서{1}'에 대해 등록 된 지시문 프로세서'{2}'<br /><br /> {3}|지시문 프로세서 유형에 해당 어셈블리에서 로드할 수 없을 때 발생 합니다. 메시지 형식, 어셈블리 및 지시문 프로세서의 이름을 제공합니다.|vshost 레지스트리에서 지시문 프로세서 정보를 (이름, 어셈블리 및 형식)를 찾습니다. 지시문 프로세서 올바르게 등록 되어 있고 해당 형식이 어셈블리에 존재 해야 합니다.|  
|어셈블리를 로드 하는 문제가 발생 했습니다 '{0}'|어셈블리를 로드 하는 문제가 발생할 때 발생 합니다. 메시지에는 어셈블리의 이름을 제공합니다.|로드 되도록 어셈블리를 지정할 수 있습니다 \<@# assembly #> 지시문 및 지시문 프로세서. 이 문자열 뒤에 오는 오류 메시지는 어셈블리 로드 실패 한 이유에서 더 많은 데이터를 제공 해야 합니다.|  
|문제 만들기 및 지시문 프로세서를 초기화 했습니다 '{1}'. 프로세서의 형식이 {0}합니다. 지시문은 무시 됩니다.|시스템 만들거나 수 없습니다. 지시문 프로세서를 초기화할 때 발생 합니다. 메시지는 지시문의 이름 및 줄 번호 및 프로세서의 형식을 제공합니다.|올바른 지시문 프로세서를 사용 하 고 지시문 프로세서에 공용 기본 생성자가 있는지 확인 해야 합니다. 지시문 프로세서의 initialize () 메서드를 실패 한 이유를 확인 하려면 디버그 옵션을 사용 하십시오. 자세한 내용은 [텍스트 템플릿 문제 해결](../modeling/debugging-a-t4-text-template.md)합니다.|  
|지시문을 처리 하는 동안 예외가 발생 했습니다 '{0}'.|지시문 프로세서 지시문을 처리 하는 경우 예외를 throw 하면 발생 합니다.|지시문 프로세서에 매개 변수가 올바른지 확인 해야 합니다.|  
|호스트에서 어셈블리 참조를 확인 하는 동안 예외가 발생 했습니다. '{0}'.|호스트 어셈블리 참조를 확인 하려고 할 때 예외를 throw 하면 발생 합니다. 어셈블리를 제공 하는 메시지 문자열을 참조 합니다.|어셈블리 참조에서 가져올 \<@# assembly #> 지시문 또는 지시문 프로세서. 어셈블리 매개 변수에 제공 된 'name' 매개 변수가 정확한 지 확인 해야 합니다.|  
|지정 하려고 지원 되지 않는 {1} 값 '{0}' 지시문에 대 한 {2}|(모두에서는 생성 된 지시문 프로세서에서 파생) RequiresProvidesDirectiveProcessor로 발생 제공 하는 경우 지원 되지 않는 필요 하거나 인수를 제공 합니다.|이름을 name = 'value' 쌍에 제공 해야 합니다 필요 하 고 매개 변수가 올바른지를 제공 합니다.|



